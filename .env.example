# Optional Firebase Admin setup.
# Leave these empty to run with the built-in local JSON backend.
FIREBASE_CREDENTIALS=C:\path\to\firebase-service-account.json
FIREBASE_PROJECT_ID=your-firebase-project-id
